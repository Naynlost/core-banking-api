# API'nin /metrics ucunu (OpenTelemetry Prometheus exporter) toplar.
# Geliştirmede API host'ta çalışır; host.docker.internal konteyner içinden ona ulaşır.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: banking-api
    static_configs:
      - targets: ["host.docker.internal:5000"]
